let items = [1, 2, 3, 4, 5];

// using simple for loop
for (let i = 0; i < items.length; i++) {
    console.log(items[i]);    // 1, 2, 3, 4, 5
}

// using for-of loop
for (let y of items) {
    console.log(y);
}